using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bugSolver : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        bgSystem.isGambling = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
